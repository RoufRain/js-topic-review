/**
 * for a given string tell me whether is has even number of a characters or not
 *
 *
 *
 */
function evenSizedString(str) {
  console.log(str);
}
evenSizedString("rain");
function numberOfElement(numbers) {
  const len = numbers.length;
  return len;
}
const ress = numberOfElement([347, 345, 65, 7, 6, 8, 45]);
console.log(ress);

function lenOfArr(num) {
  const len = num.length;
  return len;
}
const res = lenOfArr([347, 345, 65, 7, 6, 8]);
console.log(res);
//get age
function getAge(person) {
  const age = person.age;
  return age;
}
const result = getAge({ age: 25 });
console.log(result);
